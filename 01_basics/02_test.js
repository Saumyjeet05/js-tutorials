const accountId = 3456
let accountEmail="hello@gmail"
var accountPassword ="8787"
accountCity = "gkp"
// never use var for variable declaration because of its scope issue and hoisting issue and also it can be redeclared and updated but let and const cannot be redeclared but can be updated except const which cannot be updated as well
console.log(accountId);
console.table([accountEmail,accountPassword,accountId,accountCity]);


